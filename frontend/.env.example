# Usado apenas para desenvolvimento local fora do Docker (npm run dev)
# Define a URL do backend para o proxy do Vite em desenvolvimento
# Em produção o nginx faz o proxy automaticamente, essa variável não é usada
VITE_API_BASE_URL=http://localhost:3001