depends_on:
  - lint

when:
  - event: pull_request
  - event: push
    branch: main

steps:
  test-backend:
    image: node:20-alpine
    commands:
      - cd backend && npm ci && npm test

  test-frontend:
    image: node:20-alpine
    commands:
      - cd frontend && npm ci && npm test
